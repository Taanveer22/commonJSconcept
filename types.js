const stronglyTyped =
  " A strongly typed language is one where the type system is strictly enforced";
console.log(stronglyTyped);
// number x = 6;
// string y = "web"
// boolean z = true
// array tools = ['vs code', 'git', 'github']
// object technology = {frontEnd : 'client side', backEnd : 'server side'}

const dynamicallyTyped =
  "A dynamically typed language is one where the type of a variable is checked during runtime rather than at compile time";
console.log(dynamicallyTyped);
let x = 95;
let y = "ecmascript";
let z = true;
const web = ["html", "css", "js"];
const tutor = { js: "sumit da", react: "jhankar vai" };
console.log(typeof x, typeof y, typeof z, typeof web, typeof tutor);
